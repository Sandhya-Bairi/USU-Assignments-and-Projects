package com.usu.command;

public class MoveCommand extends Command {
	int moveBy;

	public MoveCommand(Object[] commandParameters) {
		if (commandParameters.length > 0)
            moveBy = (int)commandParameters[0];
	}
	
	@Override
	public void execute() {
		targetDrawing.move(moveBy);
	}

}
