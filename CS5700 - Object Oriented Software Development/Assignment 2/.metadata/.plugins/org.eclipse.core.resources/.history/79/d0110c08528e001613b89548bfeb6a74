package com.usu.stockMonitoring.forms;

import java.awt.EventQueue;

import javax.swing.JFrame;
import java.awt.GridLayout;
import javax.swing.JList;
import javax.swing.JButton;

public class AddStocksForms {

	private JFrame frame;

	/**
	 * Launch the application.
	 */
	public static void main(String[] args) {
		EventQueue.invokeLater(new Runnable() {
			public void run() {
				try {
					AddStocksForms window = new AddStocksForms();
					window.frame.setVisible(true);
				} catch (Exception e) {
					e.printStackTrace();
				}
			}
		});
	}

	/**
	 * Create the application.
	 */
	public AddStocksForms() {
		initialize();
	}

	/**
	 * Initialize the contents of the frame.
	 */
	private void initialize() {
		frame = new JFrame();
		frame.setResizable(false);
		frame.setBounds(100, 100, 410, 300);
		frame.setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE);
		frame.getContentPane().setLayout(null);
		
		JList<String> list = new JList();
		list.setBounds(10, 10, 160, 250);
		frame.getContentPane().add(list);
		
		JList<String> list_1 = new JList();
		list_1.setBounds(230, 10, 160, 250);
		frame.getContentPane().add(list_1);
		
		JButton button = new JButton(">");
		button.setBounds(180, 80, 40, 20);
		frame.getContentPane().add(button);
		
		JButton button_1 = new JButton("<");
		button_1.setBounds(180, 180, 40, 20);
		frame.getContentPane().add(button_1);
	}
	
	public void displayStockSymbols() {
		frame.getContentPane().getComponent(0).set
	}
}
